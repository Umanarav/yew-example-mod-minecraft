package gay.iamyew.marijuana.tools.custom;

import net.minecraft.item.PickaxeItem;
import net.minecraft.item.ToolMaterial;

public class MarijuanaPickaxeItem extends PickaxeItem{
    public MarijuanaPickaxeItem(ToolMaterial material, int attackDamage, float attackSpeed, Settings settings) {
        super(material, attackDamage, attackSpeed, settings);
    }
}